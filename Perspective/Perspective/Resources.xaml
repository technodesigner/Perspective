<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:PerspectiveDemo">

    <Style x:Key="GroupBoxStyle" TargetType="{x:Type GroupBox}">
        <Setter Property="Margin" Value="4.0" />
        <Setter 
            Property="BorderBrush" 
            Value="{DynamicResource {ComponentResourceKey {x:Type local:ResourceKeys}, BorderBrush}}" />
        <Setter 
            Property="Foreground" 
            Value="{DynamicResource {ComponentResourceKey {x:Type local:ResourceKeys}, PanelForegroundBrush}}" />
    </Style>

    <Style x:Key="PanelStyle" >
        <Setter 
            Property="Panel.Background" 
            Value="{DynamicResource {ComponentResourceKey {x:Type local:ResourceKeys}, PanelBackgroundBrush}}" />
    </Style>

    <Style x:Key="ViewStyle" >
        <Setter 
            Property="TreeView.Background" 
            Value="{DynamicResource {ComponentResourceKey {x:Type local:ResourceKeys}, ViewBackgroundBrush}}" />
    </Style>
</ResourceDictionary>